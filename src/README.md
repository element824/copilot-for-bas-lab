# Source code

This folder is intentionally empty for the demo. It exists so we can show Copilot answering:

> *"Does the code in `/src` implement the requirement in `spec/03-user-stories/US-001-record-consent.md`?"*

When we begin the trial properly, the Angular front-end and API code for the Patient Consent Service will live here, side-by-side with the spec.
